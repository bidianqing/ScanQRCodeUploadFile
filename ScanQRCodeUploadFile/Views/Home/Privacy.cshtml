@{
    ViewData["Title"] = "Privacy Policy";
    string connectionId = Context.Request.Query["connectionId"];
}
<h1>@ViewData["Title"]</h1>

<div id="app">
    <el-button type="primary" @@click="upload">模拟图片上传</el-button>
</div>

@section Scripts{ 
    <script>
        var app = new Vue({
            data: {
                connectionId: '@connectionId'
            },
            methods: {
                upload() {
                    let self = this;
                    let url = `/home/upload?connectionId=${self.connectionId}`;
                    httpClient.get(url).then(response => {
                        
                    })
                }
            }
        });
        app.$mount('#app');
        
    </script>
}
